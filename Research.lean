-- Non-gated research tier (NOT on the CI production gate). Aspirational /
-- model-level proofs, some broken against the pinned toolchain. Build with
-- `lake build Research`.
--   ReBAC — research-tier relationship-based access-control proofs (uses Mathlib)
import Rebac.core.ReBAC
